# Express App - How to Run
## Steps
1. Open terminal in this folder
2. Run: npm install
3. Run: npm run dev   (uses nodemon, auto-restarts on changes)
   OR:  npm start     (plain node)
4. Test with Postman or browser at http://localhost:3000
